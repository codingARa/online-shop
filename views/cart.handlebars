<!-- Source: https://bootsnipp.com/snippets/y22W -->
  <div class="container">
    <div class="row">
      <div class="col-xs-8">
        <div class="panel panel-info">
          <div class="panel-heading">
            <div class="panel-title">
              <div class="row">
                <div class="col-xs-6">
                  <h5><span class="glyphicon glyphicon-shopping-cart"></span>Shopping Cart</h5>
                </div>
              </div>
            </div>
          </div>

        {{#each cartitem}}
          <div class="panel-body">
            <div class="row">
              <div class="col-xs-2"><img class="img-responsive" src="{{cartitem.image}}">
              </div>
              <div class="col-xs-4">
                <h4 class="product-name"><strong>{{cartitem.title}}</strong></h4><h4><small>{{cartitem.description}}</small></h4>
              </div>
              <div class="col-xs-6">
                <div class="col-xs-6 text-right">
                  <h6><strong>{{cartitem.price}} <span class="text-muted">€</span></strong></h6>
                </div>
                <div class="col-xs-4">
                  <input type="text" class="form-control input-sm" value="1">
                </div>
                <div class="col-xs-2">
                  <button type="button" class="btn btn-link btn-xs">
                    <span class="glyphicon glyphicon-trash"> </span>
                  </button>
                </div>
              </div>
            </div>
            <hr>
          {{else}}
            <p>Noch haben Sie nichts in den Einkaufskorb gelegt...</p>
          {{/each}}
            <hr>
          </div>

          <div class="panel-footer">
            <div class="row text-center">
              <div class="col-xs-3">
                <button type="button" class="btn btn-success btn-block">
                  Checkout
                </button>
              </div>
              <div class="col-xs-9">
                <h4 class="text-right">Total: <strong>{{total}}</strong> €</h4>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>