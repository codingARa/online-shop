{{#each Stock}}
<div class="row">
  <div class="col-lg-4 col-md-6 mb-4">
    <div class="card h-100">
      <a href="#"><img class="card-img-top" src="./img/{{image}}" alt=""></a>
      <div class="card-body">
        <h4 class="card-title">
          <a href="#">{{title}}</a>
        </h4>
        <h5>{{price}} €</h5>
        <p class="card-text">{{description}}</p>
      </div>
    </div>
  </div>
</div>
{{else}}
  <p>Leider sind wir vollkommen ausverkauft.</p>
{{/each}}